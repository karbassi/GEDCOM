# README + CLI docs

Status: done
Triage: ready-for-agent
Type: AFK

## What to build

User-facing documentation for the CLI: a README section (install, the three subcommands, the authoring-dialect reference with a worked example) and a note on the optional `[yaml]` extra. Update the top-level project README status to mention the CLI.

## Acceptance criteria

- [ ] README documents `build`/`validate`/`init` with examples.
- [ ] The authoring dialect (top-level keys, references, dates, enums) is documented.
- [ ] The optional `gedcom7[yaml]` extra is explained.

## Blocked by

- 09 (init scaffold) so docs and the template agree
