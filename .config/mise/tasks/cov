#!/usr/bin/env bash
#MISE description="Run the test suite under coverage; fail under 100% (extra args pass to pytest)"
set -euo pipefail
# `python -m` so coverage/pytest run in the env with the editable install.
uv run python -m coverage run -m pytest "$@"
uv run python -m coverage report
