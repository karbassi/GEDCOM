#!/usr/bin/env bash
#MISE description="Run the test suite (extra args pass through to pytest)"
set -euo pipefail
# Invoke via `python -m` so the project's editable install is on the path;
# the bare `pytest`/`mypy` console scripts can resolve to an env without it.
uv run python -m pytest "$@"
